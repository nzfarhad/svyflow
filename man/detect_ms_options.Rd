% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiselect.R
\name{detect_ms_options}
\alias{detect_ms_options}
\title{Detect already-expanded multi-select option columns}
\usage{
detect_ms_options(df, vars)
}
\arguments{
\item{df}{Data frame.}

\item{vars}{Character vector of \code{select_multiple} variable names to look
up.}
}
\value{
A named list. Each element is a character vector of detected
option column names for the corresponding variable (empty if none
detected).
}
\description{
Kobo and SurveyCTO exports commonly include one binary (0/1) column per
option for every \code{select_multiple} question, named like \code{var/opt},
\code{var.opt}, or \code{var___opt}. This helper scans a data frame for those
sibling columns so callers know whether \code{\link[=expand_multiselect]{expand_multiselect()}} needs to
run.
}
\examples{
df <- data.frame(
  `needs/cash` = c(1L, 0L), `needs/food` = c(0L, 1L),
  check.names = FALSE
)
detect_ms_options(df, "needs")

}
\seealso{
\code{\link[=expand_multiselect]{expand_multiselect()}}
}
