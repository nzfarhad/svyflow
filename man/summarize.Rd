% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarize}
\alias{summarize}
\alias{summarize_select_one}
\alias{summarize_select_multiple}
\alias{summarize_mean}
\alias{summarize_sum}
\alias{summarize_median}
\alias{summarize_quantile}
\alias{summarize_min}
\alias{summarize_max}
\title{Single-indicator summary wrappers}
\usage{
summarize_select_one(
  design,
  variable,
  disaggregation = NULL,
  variable_label = NULL,
  disaggregation_label = NULL,
  result_format = "proportion",
  digits = 1,
  crosstab = FALSE,
  with_ci = FALSE
)

summarize_select_multiple(
  design,
  variable,
  disaggregation = NULL,
  variable_label = NULL,
  disaggregation_label = NULL,
  result_format = "proportion",
  digits = 1,
  crosstab = FALSE,
  with_ci = FALSE
)

summarize_mean(
  design,
  variable,
  disaggregation = NULL,
  variable_label = NULL,
  disaggregation_label = NULL,
  digits = NULL,
  result_only = FALSE
)

summarize_sum(
  design,
  variable,
  disaggregation = NULL,
  variable_label = NULL,
  disaggregation_label = NULL,
  digits = NULL,
  result_only = FALSE
)

summarize_median(
  design,
  variable,
  disaggregation = NULL,
  variable_label = NULL,
  disaggregation_label = NULL,
  digits = NULL,
  result_only = FALSE
)

summarize_quantile(
  design,
  variable,
  q,
  disaggregation = NULL,
  variable_label = NULL,
  disaggregation_label = NULL,
  digits = NULL,
  result_only = FALSE
)

summarize_min(
  design,
  variable,
  disaggregation = NULL,
  variable_label = NULL,
  disaggregation_label = NULL,
  digits = NULL,
  result_only = FALSE
)

summarize_max(
  design,
  variable,
  disaggregation = NULL,
  variable_label = NULL,
  disaggregation_label = NULL,
  digits = NULL,
  result_only = FALSE
)
}
\arguments{
\item{design}{A \link[srvyr:tbl_svy]{srvyr::tbl_svy} survey design (typically from
\code{\link[=make_design]{make_design()}}).}

\item{variable}{Character. The column to summarise.}

\item{disaggregation}{Character or \code{NULL}. A grouping column name; pass
\code{NULL} (or \code{"all"}) for no disaggregation.}

\item{variable_label}{Display label for \code{variable}. Used as the column
header for response/option values (categorical) or as the value of the
\code{Indicator} column (numeric). Falls back to \code{variable} when \code{NULL}.}

\item{disaggregation_label}{Display label for \code{disaggregation}. Used as
the column header for the disaggregation column in long output. Falls
back to \code{disaggregation} when \code{NULL}.}

\item{result_format, digits}{Passed through to \code{\link[=analyze_survey]{analyze_survey()}}. See
that function's documentation. Categorical wrappers use them; numeric
wrappers accept \code{digits} only.}

\item{crosstab}{If \code{TRUE} (and \code{disaggregation} is set), pivot the long
output to a wide table: rows = response / option values, columns =
disaggregation levels. SE / CI / Count are dropped from the wide view.
Categorical wrappers only.}

\item{with_ci}{If \code{TRUE} (only meaningful when \code{crosstab = TRUE}),
format cells as \code{"estimate (CI_low–CI_high)"}. Categorical wrappers
only.}

\item{result_only}{If \code{TRUE}, drop \code{SE} / \code{CI_low} / \code{CI_high} / \code{Count}
from the output. Numeric wrappers only.}

\item{q}{For \code{\link[=summarize_quantile]{summarize_quantile()}} only: a single quantile in \verb{[0, 1]}.}
}
\value{
A tibble of class \code{svyflow_summary}.
}
\description{
Ergonomic wrappers around the survey-design-aware aggregators for use on
one indicator at a time. They build a one-row analysis plan, delegate to
\code{\link[=analyze_survey]{analyze_survey()}}, and post-process the long output into a publication-
ready tibble (column names tailored to the indicator, optional crosstab
pivot when disaggregated).
}
\details{
All wrappers accept a \link[srvyr:tbl_svy]{srvyr::tbl_svy} design. The categorical wrappers
return proportion / percentage estimates with \code{SE} / \code{CI_low} / \code{CI_high}
/ \code{Count} / \code{Denominator}. The numeric wrappers return the chosen
statistic with the same auxiliary columns (except \code{Denominator}, which
equals \code{Count} for raw quantities and is dropped).
}
\seealso{
\code{\link[=analyze_survey]{analyze_survey()}}, \code{\link[=format_results]{format_results()}}
}
