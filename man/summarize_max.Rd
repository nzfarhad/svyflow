% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarize_max}
\alias{summarize_max}
\title{Unweighted maximum of a numeric indicator}
\usage{
summarize_max(
  design,
  variable,
  disaggregation = NULL,
  variable_label = NULL,
  disaggregation_label = NULL,
  digits = NULL,
  result_only = FALSE,
  ci = ci_opts()
)
}
\arguments{
\item{design}{A \link[srvyr:tbl_svy]{srvyr::tbl_svy} survey design.}

\item{variable}{Character. The numeric column to summarise.}

\item{disaggregation}{Character or \code{NULL}. A grouping column name;
\code{NULL} (or \code{"all"}) means no disaggregation.}

\item{variable_label}{Display label for \code{variable}. Becomes the value
of the \code{Indicator} column. Falls back to \code{variable} when \code{NULL}.}

\item{disaggregation_label}{Display label for \code{disaggregation}. Used as
the column header for the disaggregation column. Falls back to
\code{disaggregation} when \code{NULL}.}

\item{digits}{Non-negative numeric scalar, or \code{NULL} for no rounding.
Passed through to \code{\link[=analyze_survey]{analyze_survey()}}; numeric statistics are not
rescaled, so rounding only takes effect in the \code{percent_fmt} form
(not generally useful here). Default \code{NULL}.}

\item{result_only}{If \code{TRUE}, return only \code{Indicator}, the disaggregation
column (if any), and the \code{Mean} column. \code{SE} / \verb{CI_*} / \code{Count} are
dropped.}

\item{ci}{A \code{\link[=ci_opts]{ci_opts()}} bundle controlling the confidence-interval
method. For numeric statistics the relevant knobs are the universal
\code{ci_level} / \code{df} (e.g. \code{ci_opts(ci_level = 0.90, df = Inf)} for a
90\% normal-approximation interval); for quantiles also
\code{interval_type} / \code{qrule}. Defaults to \code{ci_opts()}.}
}
\value{
A \code{svyflow_summary} tibble with \code{Indicator}, optionally the
disaggregation column, then \code{Max} (and \code{NA} \code{SE} / \verb{CI_*} / \code{Count}
unless \code{result_only = TRUE}).
}
\description{
Reports the raw maximum, unweighted by design. See \code{\link[=summarize_min]{summarize_min()}}
for the rationale. Value column is named \code{Max}.
}
\examples{
df  <- data.frame(income = c(150, 320, 0, 980))
des <- make_design(df)
summarize_max(des, "income", variable_label = "Income")

}
\seealso{
\code{\link[=summarize_min]{summarize_min()}}

Other summarize: 
\code{\link{summarize_mean}()},
\code{\link{summarize_median}()},
\code{\link{summarize_min}()},
\code{\link{summarize_quantile}()},
\code{\link{summarize_select_multiple}()},
\code{\link{summarize_select_one}()},
\code{\link{summarize_sum}()}
}
\concept{summarize}
