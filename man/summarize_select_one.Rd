% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarize_select_one}
\alias{summarize_select_one}
\title{Summarise a single-choice (select_one) categorical indicator}
\usage{
summarize_select_one(
  design,
  variable,
  disaggregation = NULL,
  variable_label = NULL,
  disaggregation_label = NULL,
  result_format = "proportion",
  digits = 1,
  crosstab = FALSE,
  with_ci = FALSE,
  ci = ci_opts()
)
}
\arguments{
\item{design}{A \link[srvyr:tbl_svy]{srvyr::tbl_svy} survey design (typically from
\code{\link[=make_design]{make_design()}}).}

\item{variable}{Character. The column to summarise.}

\item{disaggregation}{Character or \code{NULL}. A grouping column name; pass
\code{NULL} (or \code{"all"}) for no disaggregation.}

\item{variable_label}{Display label for \code{variable}. Used as the column
header for response values. Falls back to \code{variable} when \code{NULL}.}

\item{disaggregation_label}{Display label for \code{disaggregation}. Used as
the column header for the disaggregation column in long output. Falls
back to \code{disaggregation} when \code{NULL}.}

\item{result_format, digits}{Passed through to \code{\link[=analyze_survey]{analyze_survey()}}.
\code{result_format} is \code{"proportion"} (default; 0-1 numeric, value column
named \code{Proportion}), \code{"percent"} (0-100 numeric, \code{Percentage}) or
\code{"percent_fmt"} (character \code{"53.3\%"}, \code{Percentage}). \code{digits} is the
rounding applied to the percent modes; default \code{1}.}

\item{crosstab}{If \code{TRUE} (and \code{disaggregation} is set), pivot the long
output to a wide table: rows = response values, columns =
disaggregation levels. \code{SE} / \verb{CI_*} / \code{Count} / \code{Denominator} are
dropped from the wide view.}

\item{with_ci}{If \code{TRUE} (only meaningful when \code{crosstab = TRUE}),
format wide-table cells as \code{"estimate (CI_low-CI_high)"}.}

\item{ci}{A \code{\link[=ci_opts]{ci_opts()}} bundle controlling the confidence-interval
method. For proportions the relevant knob is \code{prop_method} (e.g.
\code{ci_opts(prop_method = "logit")} for bounded intervals on rare
outcomes), plus the universal \code{ci_level} / \code{df}. Defaults to
\code{ci_opts()} (95\% t-interval, plain Wald proportions).}
}
\value{
A tibble of class \code{svyflow_summary}. Schema depends on
\code{disaggregation} and \code{crosstab}:
\itemize{
\item No disaggregation:
\verb{<variable_label>} | \code{Proportion}/\code{Percentage} | \code{SE} | \code{CI_low} |
\code{CI_high} | \code{Count} | \code{Denominator}
\item Disaggregation, long (\code{crosstab = FALSE}):
\verb{<variable_label>} | \verb{<disaggregation_label>} | \code{Proportion} | ...
\item Disaggregation, \code{crosstab = TRUE}:
\verb{<variable_label>} | \if{html}{\out{<one column per disaggregation level>}}
}
}
\description{
Builds a one-row analysis plan and delegates to \code{\link[=analyze_survey]{analyze_survey()}}, then
reshapes the long output into a publication-ready table: one row per
response value, with \code{variable_label} as the row-header column name and
\code{Result} renamed to \code{Proportion} or \code{Percentage} depending on
\code{result_format}. Optionally pivots a disaggregated result into a
crosstab.
}
\examples{
df  <- data.frame(
  edu_lvl = sample(c("none","primary","secondary"), 200, TRUE),
  gender  = sample(c("m","f"), 200, TRUE),
  weight  = runif(200, 0.5, 2.0)
)
des <- make_design(df, weights = "weight")

summarize_select_one(des, "edu_lvl", variable_label = "Education")
summarize_select_one(des, "edu_lvl",
                     disaggregation = "gender",
                     variable_label = "Education",
                     disaggregation_label = "Sex",
                     crosstab = TRUE, with_ci = TRUE)

}
\seealso{
\code{\link[=summarize_select_multiple]{summarize_select_multiple()}}, \code{\link[=analyze_survey]{analyze_survey()}},
\code{\link[=format_results]{format_results()}}

Other summarize: 
\code{\link{summarize_max}()},
\code{\link{summarize_mean}()},
\code{\link{summarize_median}()},
\code{\link{summarize_min}()},
\code{\link{summarize_quantile}()},
\code{\link{summarize_select_multiple}()},
\code{\link{summarize_sum}()}
}
\concept{summarize}
