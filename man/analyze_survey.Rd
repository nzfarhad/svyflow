% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{analyze_survey}
\alias{analyze_survey}
\title{Run an analysis plan against a survey design}
\usage{
analyze_survey(
  design,
  analysis_plan,
  multi_response_sep = "; ",
  result_format = "proportion",
  digits = 1,
  use_labels = TRUE,
  ci = ci_opts()
)
}
\arguments{
\item{design}{A \link[srvyr:tbl_svy]{srvyr::tbl_svy} survey design (typically from
\code{\link[=make_design]{make_design()}}).}

\item{analysis_plan}{Data frame describing the analyses to run. Columns:
\itemize{
\item \code{variable}: column to aggregate.
\item \code{kobo_type}: one of \code{"select_one"}, \code{"select_multiple"}, \code{"integer"}.
\item \code{aggregation_method}: only consulted for \code{kobo_type == "integer"};
one of \code{"mean"}, \code{"median"}, \code{"sum"}, \code{"firstq"}, \code{"thirdq"},
\code{"min"}, \code{"max"}.
\item \code{disaggregation}: a column name, \code{"all"}, or \code{NA}.
\item \code{variable_label} (optional): display label substituted into the
\code{Question} column when \code{use_labels = TRUE}. \code{NA} falls back to
\code{variable}.
\item \code{disaggregation_label} (optional): display label substituted into
the \code{Disaggregation} column when \code{use_labels = TRUE}. \code{NA} falls
back to \code{disaggregation} (including the literal \code{"all"}).
\item \code{repeat_for} (optional): a column name to add a second
disaggregation axis, or \code{NA}.
\item \code{group} (optional): a section name for the question. When present it
is carried into the result as a \code{Group} column and used as a
section separator by \code{\link[=write_xlsx]{write_xlsx()}}. Does not affect estimates.
}}

\item{multi_response_sep}{Separator used by \code{\link[=expand_multiselect]{expand_multiselect()}} if it
needs to be invoked on the design's data. Defaults to \code{"; "}.}

\item{result_format}{How proportion-producing rows (\code{select_one},
\code{select_multiple}) report \code{Result}/\code{SE}/\verb{CI_*}. One of \code{"proportion"}
(default; 0-1 numeric), \code{"percent"} (0-100 numeric), or
\code{"percent_fmt"} (character \code{"53.3\%"}; coerces the \code{Result} column
to character for non-proportion rows too, so the column type stays
stable). Non-proportion rows (mean, sum, median, etc.) report raw
values regardless of this setting.}

\item{digits}{Non-negative numeric scalar, or \code{NULL} for no rounding.
Applied to numeric outputs in the two percent modes; ignored in
\code{"proportion"} mode so the default does not crush precision. Default
\code{1}.}

\item{use_labels}{If \code{TRUE} (the default), substitute \code{variable_label}
into the output's \code{Question} column and \code{disaggregation_label} into
\code{Disaggregation} whenever those columns exist on the plan and the
per-row value is not \code{NA}. If \code{FALSE}, the raw column names are used.
Plans without the label columns are unaffected (backward compatible).}

\item{ci}{A \code{\link[=ci_opts]{ci_opts()}} bundle controlling the confidence-interval
method (confidence level, degrees of freedom, proportion CI method,
quantile interval). Defaults to \code{ci_opts()}, which reproduces the
historical behaviour (95\% t-interval on the design df, plain Wald
proportions). The method-specific knobs only affect the rows they
apply to (\code{prop_method} -> \code{select_one} / \code{select_multiple},
\code{interval_type} / \code{qrule} -> quantile rows); others are ignored.}
}
\value{
A \code{\link{svyflow_results}} tibble with columns:
\code{Disaggregation}, \code{Disaggregation_level}, \code{Question}, \code{Response},
\code{Aggregation_method}, \code{Result}, \code{SE}, \code{CI_low}, \code{CI_high}, \code{Count},
\code{Denominator}, \code{repeat_for}.
}
\description{
The public entry point. Validates the plan, resolves multi-select option
columns (using the \code{"ms_options"} attribute on the design's data if
present, otherwise detecting siblings), and walks every plan row
producing one or more result rows. Single disaggregation iterates over
\code{unique(df[[disag]])}. Double disaggregation (the optional \code{repeat_for}
column on the plan) iterates over \code{unique(df[[repeat_for]])} on the
outside before the per-row disaggregation.
}
\examples{
df <- data.frame(
  gender = sample(c("m","f"), 100, TRUE),
  age    = round(rnorm(100, 35, 8))
)
ap <- data.frame(
  variable = c("gender", "age"),
  kobo_type = c("select_one", "integer"),
  aggregation_method = c(NA, "mean"),
  disaggregation = c("all", "gender")
)
analyze_survey(make_design(df), ap)

}
\seealso{
\code{\link[=make_design]{make_design()}}, \code{\link[=expand_multiselect]{expand_multiselect()}}, \code{\link[=validate_plan]{validate_plan()}}
}
