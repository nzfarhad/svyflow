% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design.R
\name{make_design}
\alias{make_design}
\title{Build a survey design}
\usage{
make_design(
  df,
  weights = NULL,
  strata = NULL,
  ids = NULL,
  fpc = NULL,
  nest = FALSE
)
}
\arguments{
\item{df}{Data frame containing the survey responses.}

\item{weights}{Name of the column with sampling weights, or \code{NULL}.}

\item{strata}{Name of the column identifying strata, or \code{NULL}.}

\item{ids}{Name of the column identifying primary sampling units, or
\code{NULL} (defaults to \code{~1}, i.e. independent observations).}

\item{fpc}{Name of the finite-population correction column, or \code{NULL}.}

\item{nest}{If \code{TRUE}, relabel cluster ids to be nested within strata.
Passed through to \code{\link[survey:svydesign]{survey::svydesign()}}.}
}
\value{
A \link[srvyr:tbl_svy]{srvyr::tbl_svy} object.
}
\description{
Thin convenience wrapper around \code{\link[survey:svydesign]{survey::svydesign()}} that accepts column
names as strings and wraps the result as a \link[srvyr:tbl_svy]{srvyr::tbl_svy} so it can be
used downstream with the tidy verbs.
}
\details{
If \code{weights}, \code{strata}, \code{ids}, and \code{fpc} are all \code{NULL}, the returned design
is a simple random sample - useful for non-survey data or for running the
same analysis plan unweighted as a baseline.
}
\examples{
df <- data.frame(
  x = rnorm(10), s = rep(c("a","b"), each = 5), w = runif(10, 0.5, 1.5)
)
make_design(df)                                    # SRS
make_design(df, weights = "w")                     # weighted
make_design(df, weights = "w", strata = "s")       # stratified

}
\seealso{
\code{\link[=analyze_survey]{analyze_survey()}}, \code{\link[=expand_multiselect]{expand_multiselect()}}
}
