% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-results.R
\name{format_results}
\alias{format_results}
\title{Reformat an existing svyflow_results tibble}
\usage{
format_results(x, to, from = NULL, digits = 1)
}
\arguments{
\item{x}{A \code{\link{svyflow_results}} tibble, or any data frame carrying the
public columns \code{Result}, \code{SE}, \code{CI_low}, \code{CI_high}, \code{Aggregation_method}.}

\item{to}{Target format: \code{"proportion"} (0-1 numeric), \code{"percent"} (0-100
numeric), or \code{"percent_fmt"} (character \code{"53.3\%"}).}

\item{from}{Source format. If \code{NULL} (the default), read from
\code{attr(x, "result_format")} when present; otherwise inferred from the
type / range of the \code{Result} column on proportion rows.}

\item{digits}{Non-negative numeric scalar, or \code{NULL} for no rounding.
Applied when \code{to} is \code{"percent"} or \code{"percent_fmt"}; ignored for
\code{"proportion"} so a default of \code{1} does not crush precision.}
}
\value{
A \code{svyflow_results} tibble with the four numeric/character
columns reformatted. Attributes \code{result_format} and \code{digits} are
refreshed to reflect the new state.
}
\description{
Converts the \code{Result}, \code{SE}, \code{CI_low}, \code{CI_high} columns between the three
supported representations (\code{"proportion"}, \code{"percent"}, \code{"percent_fmt"})
\emph{after} the analysis has run. The motivation is performance: re-running
\code{\link[=analyze_survey]{analyze_survey()}} just to change presentation is wasteful when the plan
is large, and \code{format_results()} is a cheap post-processing pass.
}
\details{
Aggregation-method aware: only rows where \code{Aggregation_method == "perc"}
(i.e. produced by \code{select_one} / \code{select_multiple} aggregators) are
rescaled and \verb{\%}-suffixed. Rows for \code{mean}, \code{sum}, \code{median}, \verb{1st_Qu},
\verb{3rd_Qu}, \code{min_unweighted}, \code{max_unweighted} carry raw quantities; their
values are only type-coerced (character <-> numeric) when the target
format demands it, with no scaling and no \verb{\%}.

Round-trip caveat: \code{"percent_fmt"} is a rounded character form, so
\code{percent_fmt -> proportion} recovers the rounded value, not the original
full-precision proportion.
}
\examples{
df <- data.frame(
  gender = sample(c("m","f"), 200, TRUE),
  age    = round(rnorm(200, 35, 8))
)
ap <- data.frame(
  variable = c("gender", "age"),
  kobo_type = c("select_one", "integer"),
  aggregation_method = c(NA, "mean"),
  disaggregation = c("all", "gender")
)
res <- analyze_survey(make_design(df), ap)           # proportion (default)
format_results(res, to = "percent")                  # 0-100 numeric
format_results(res, to = "percent_fmt", digits = 2)  # "53.27\%"

}
\seealso{
\code{\link[=analyze_survey]{analyze_survey()}}
}
