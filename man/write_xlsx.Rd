% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-xlsx.R
\name{write_xlsx}
\alias{write_xlsx}
\alias{write_xlsx.svyflow_results}
\title{Export svyflow results to a styled Excel workbook}
\usage{
write_xlsx(x, file, ...)

\method{write_xlsx}{svyflow_results}(
  x,
  file,
  theme = xlsx_theme(),
  overall_sheet = TRUE,
  overall_label = "Overall",
  long_sheet = TRUE,
  long_label = "Long",
  with_ci = FALSE,
  with_counts = c("none", "row_label", "inline", "parallel"),
  col_width = 21,
  ...
)
}
\arguments{
\item{x}{A \code{\link{svyflow_results}} tibble from \code{\link[=analyze_survey]{analyze_survey()}} (or any data
frame with the public columns, optionally a \code{Group} column).}

\item{file}{Output path ending in \code{.xlsx}.}

\item{...}{Unused; for S3 compatibility.}

\item{theme}{An \code{\link[=xlsx_theme]{xlsx_theme()}} object controlling fonts, colours and
fills. Defaults to the standard publication theme.}

\item{overall_sheet}{If \code{TRUE} (default), add a dedicated sheet holding
the un-disaggregated results for every question. Placed first in the
workbook.}

\item{overall_label}{Row/sheet label for the un-disaggregated results.
Default \code{"Overall"}.}

\item{long_sheet}{If \code{TRUE} (default), append a sheet containing the
full long-form input (every column of \code{x}, including any
\code{repeat_for} rows) as a single flat table. Useful as the data source
for downstream analysis. Placed last in the workbook.}

\item{long_label}{Sheet name for the long-form sheet. Default \code{"Long"}.}

\item{with_ci}{If \code{TRUE}, compose each value cell as
\code{"<estimate> (<CI_low> - <CI_high>)"} using the values exactly as they
appear in \code{x} (no re-scaling or re-rounding). Set the precision /
scale upstream with \code{\link[=format_results]{format_results()}}. Rows without confidence
intervals (\code{min} / \code{max}) fall back to a plain estimate. Default
\code{FALSE}.}

\item{with_counts}{How to display unweighted counts. One of:
\itemize{
\item \code{"none"} (default) — omit counts entirely.
\item \code{"row_label"} — append \verb{ (n=<Denominator>)} to every row label
(disaggregation levels and the Overall row), using the level's
total valid n.
\item \code{"inline"} — same row-label suffix as \code{"row_label"} \strong{plus}
\verb{ (n=<Count>)} appended to every value cell using the per-cell
unweighted Count. Combines with \code{with_ci = TRUE} as
\code{"<est> (<lo> - <hi>; n=<Count>)"}.
\item \code{"parallel"} — same row-label suffix as \code{"row_label"} \strong{plus} a
sibling \code{"(n)"} column right after every response/value column
carrying the per-cell Count.
Requires the relevant column (\code{Denominator} / \code{Count}) on \code{x}; missing
or \code{NA} values are silently skipped.
}}

\item{col_width}{Fixed width (Excel character-width units, ~9.3 px per
unit at Calibri 11 pt) applied to every value column. Default \code{21}
(~196 px) — comfortable for \code{"<est> (<lo> - <hi>)"} with one decimal.
The row-label (first) column always sizes itself to its content.
Long column \strong{headers wrap} within the fixed width automatically.}
}
\value{
The output \code{file} path, invisibly.
}
\description{
Writes the long output of \code{\link[=analyze_survey]{analyze_survey()}} to an \code{.xlsx} file as
publication-ready crosstabs: \strong{one sheet per disaggregation variable},
plus an \strong{Overall} sheet for the un-disaggregated (\code{"all"}) results.
Within each sheet, one crosstab block per question is stacked in plan
order, with \strong{disaggregation levels as rows and an \code{Overall} summary row
at the bottom} of each block. If the analysis plan supplied a \code{group}
column, its values become \strong{section-separator headers} between blocks.
}
\details{
Values are written \strong{exactly as they appear in \code{x}} — no number
formatting is imposed. Pass the default proportion output for 0-1 values,
or run \code{\link[=format_results]{format_results()}} first for percentages / formatted percentages.

Styling follows a clean publication theme by default; override via
\code{\link[=xlsx_theme]{xlsx_theme()}}.

Double-disaggregation (\code{repeat_for}) rows are not included in this export.
}
\examples{
\donttest{
df <- data.frame(
  gender = sample(c("m", "f"), 200, TRUE),
  edu    = sample(c("none", "primary", "secondary"), 200, TRUE),
  weight = runif(200, 0.5, 2)
)
ap <- tibble::tribble(
  ~variable, ~kobo_type,   ~aggregation_method, ~disaggregation, ~group,
  "edu",     "select_one", NA,                  "all",           "Education",
  "edu",     "select_one", NA,                  "gender",        "Education"
)
res <- analyze_survey(make_design(df, weights = "weight"), ap)
write_xlsx(res, tempfile(fileext = ".xlsx"))
}

}
\seealso{
\code{\link[=analyze_survey]{analyze_survey()}}, \code{\link[=format_results]{format_results()}}, \code{\link[=xlsx_theme]{xlsx_theme()}}
}
