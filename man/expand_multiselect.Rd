% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiselect.R
\name{expand_multiselect}
\alias{expand_multiselect}
\title{Expand multi-select string columns into binary option columns}
\usage{
expand_multiselect(df, vars, sep = "; ")
}
\arguments{
\item{df}{Data frame.}

\item{vars}{Character vector of multi-select variable names.}

\item{sep}{Separator between options in the source string column.
Defaults to \code{"; "}.}
}
\value{
The input data frame, possibly with new binary columns appended,
and an \code{ms_options} attribute mapping each variable to its option
column names.
}
\description{
Splits each named "; "-separated string column into one 0/1 column per
unique option (Kobo convention \code{var___opt}), but only for variables that
do \strong{not} already have sibling binary columns (\code{var/opt}, \code{var.opt},
\code{var___opt}). The detected or newly-created option columns are recorded
on the result as \code{attr(df, "ms_options")} so \code{\link[=analyze_survey]{analyze_survey()}} can find
them without rescanning.
}
\details{
If a generated column name would collide with an existing column, a
numeric suffix (\verb{_x1}, \verb{_x2}, ...) is appended and a warning is emitted.
}
\examples{
df <- data.frame(needs = c("cash; food", "food", NA, "shelter; cash"))
out <- expand_multiselect(df, vars = "needs")
attr(out, "ms_options")

}
\seealso{
\code{\link[=detect_ms_options]{detect_ms_options()}}, \code{\link[=analyze_survey]{analyze_survey()}}
}
