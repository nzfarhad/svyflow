% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{compare_groups}
\alias{compare_groups}
\title{Compare groups with the right survey-design-aware significance test}
\usage{
compare_groups(
  design,
  variables,
  group,
  test = c("auto", "ttest", "paired_ttest", "wilcoxon", "anova", "kruskal", "chisq",
    "fisher", "prop_z"),
  paired = FALSE,
  pair_by = NULL,
  parametric = TRUE,
  small_sample = "auto",
  variable_labels = NULL,
  group_label = NULL,
  digits = 3,
  ci = ci_opts()
)
}
\arguments{
\item{design}{A \link[srvyr:tbl_svy]{srvyr::tbl_svy} survey design (typically from
\code{\link[=make_design]{make_design()}}).}

\item{variables}{Character vector of one or more indicator column names.}

\item{group}{Character. The grouping column name.}

\item{test}{Which test to run. \code{"auto"} (default) resolves per the decision
tree above; otherwise one of \code{"ttest"}, \code{"paired_ttest"}, \code{"wilcoxon"},
\code{"anova"}, \code{"kruskal"}, \code{"chisq"}, \code{"fisher"}, \code{"prop_z"}.}

\item{paired}{Logical. For a numeric two-group comparison, run the paired
variant. Requires \code{pair_by}. Default \code{FALSE}.}

\item{pair_by}{Character. The per-unit identifier column linking the two
observations of a pair (e.g. a household ID measured at baseline and
endline). Required when the resolved test is paired.}

\item{parametric}{Logical. Use the parametric numeric test (t-test / ANOVA)
rather than its rank-based counterpart (Wilcoxon / Kruskal-Wallis).
Default \code{TRUE}. Ignored for categorical indicators.}

\item{small_sample}{\code{"auto"} (default), \code{TRUE}, or \code{FALSE}. When \code{"auto"}
or \code{TRUE}, a categorical comparison switches from chi-square to Fisher's
exact if any expected cell count is below 5. \code{FALSE} always keeps the
chi-square.}

\item{variable_labels}{Optional named character vector of display labels;
names must be a subset of \code{variables}. Used for the \code{Indicator} column.}

\item{group_label}{Optional single string used for the \code{Group} column.
Defaults to the \code{group} column name.}

\item{digits}{Number of decimal places for the rounding applied to the
numeric output columns. Default \code{3}.}

\item{ci}{A \code{\link[=ci_opts]{ci_opts()}} bundle. \code{ci_level} and \code{df} feed the t / paired-t
confidence intervals and p-values (e.g. \code{ci_opts(df = Inf)} for a
normal-approximation interval). Default \code{ci_opts()}.}
}
\value{
A tibble of class \code{svyflow_summary}, one row per indicator, with
columns \code{Indicator}, \code{Group}, \code{Comparison}, \code{Test}, \code{Statistic}, \code{DF},
\code{DF2}, \code{Estimate}, \code{CI_low}, \code{CI_high}, \code{Effect_size},
\code{Effect_size_type}, \code{P_value}, \code{Significance}, \code{N}.
}
\description{
Selects and runs an appropriate hypothesis test for each indicator,
comparing it across the levels of a grouping variable, and returns a tidy
one-row-per-indicator table. With \code{test = "auto"} (the default) the test is
chosen from the indicator type, the number of groups, and the \code{paired} /
\code{parametric} flags:
}
\details{
\describe{
\item{Numeric indicator, 2 groups, independent}{\code{ttest} (parametric) or
\code{wilcoxon} (Mann-Whitney U).}
\item{Numeric indicator, 2 groups, paired}{\code{paired_ttest} (parametric) or
paired \code{wilcoxon} (signed-rank). Requires \code{pair_by}.}
\item{Numeric indicator, 3+ groups}{\code{anova} (parametric) or \code{kruskal}
(Kruskal-Wallis).}
\item{Categorical indicator}{\code{chisq} (Rao-Scott F chi-square). Switches
to \code{fisher} (Fisher's exact) when any expected cell count is below 5
and \code{small_sample != FALSE}.}
}

Any branch can be forced with \code{test}. \code{prop_z} (a two-proportion z-test) is
never picked automatically -- it is mathematically a 2x2 chi-square -- and
must be requested explicitly.

All tests except \code{fisher} respect the survey design (weights, strata,
clusters). Fisher's exact has no survey-aware form, so it is run on the
\strong{unweighted} contingency table; a note is emitted when it is used.
}
\section{Test statistics and degrees of freedom}{

The \code{Statistic}, \code{DF}, \code{DF2} and \code{P_value} columns come straight from the
design-based engine (\code{survey::svyttest()}, \code{svyglm()} +
\code{regTermTest()}, \code{svychisq()}, \code{svyranktest()}), so weights, strata and
clusters are fully respected. The independent t-test matches
\code{survey::svyttest()} exactly. \code{prop_z} is, by definition, a normal-theory
z-test (\code{DF = NA}): with few PSUs a design-df t-test (\code{test = "ttest"})
is the safer choice. Fisher's exact has no survey-aware form, so it runs
on the \strong{unweighted} table and a warning is emitted when it is selected
automatically.
}

\section{Effect sizes}{

Effect sizes are descriptive magnitudes reported alongside each test.
Most are design-consistent; two are documented approximations:
\describe{
\item{\code{Cohen_d} (t-test, paired t-test)}{Mean difference over the
pooled standard deviation. The group variances are design-based
(\code{srvyr::survey_var()}); they are pooled using the unweighted group
degrees of freedom, so \code{d} is a design-informed but not fully
design-weighted standardisation.}
\item{\code{Rank_biserial} (Wilcoxon)}{Design-consistent: \eqn{r = 1 - 2
      \hat{P}(Y > X)} for the unpaired case, with \eqn{\hat{P}} estimated
from the sampling weights. The paired case weights each pair's signed
rank contribution by its sampling weight (the ranks themselves are
unweighted -- an approximation).}
\item{\code{Eta_sq} (ANOVA)}{An \strong{approximate} design-informed
eta-squared from per-group design-based means and variances combined
with unweighted group sizes. For designs with highly variable weights
it can over- or understate the effect; treat it as indicative.}
\item{\code{Epsilon_sq} (Kruskal-Wallis)}{Derived from the design-based
\eqn{H} statistic.}
\item{\code{Cramer_V} (chi-square)}{Design-consistent: computed from the
weighted contingency proportions (scaled to \code{n}), bounded in
\eqn{[0, 1]}. For the Fisher r x c fallback it is computed from the
unweighted table, matching the unweighted test.}
\item{\code{Odds_ratio} (Fisher 2x2), \code{Cohen_h} (prop_z)}{From the Fisher
result and the two design-weighted proportions respectively.}
}
For the Wilcoxon test, \code{Estimate} is the difference of the design-weighted
group medians, not the Hodges-Lehmann estimator.
}

\examples{
df  <- data.frame(
  hh_size = stats::rpois(200, 5) + 1,
  gender  = sample(c("m", "f"), 200, TRUE),
  region  = sample(c("n", "s", "e"), 200, TRUE),
  weight  = runif(200, 0.5, 2)
)
des <- make_design(df, weights = "weight")

# Auto: numeric x 2 groups -> t-test
compare_groups(des, "hh_size", "gender")

# Auto: numeric x 3 groups -> ANOVA
compare_groups(des, "hh_size", "region")

# Rank-based variants
compare_groups(des, "hh_size", "gender", parametric = FALSE)

}
\seealso{
\code{\link[=analyze_survey]{analyze_survey()}}, \code{\link[=summarize_mean]{summarize_mean()}}, \code{\link[=ci_opts]{ci_opts()}}
}
