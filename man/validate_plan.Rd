% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_plan}
\alias{validate_plan}
\title{Validate an analysis plan against a data frame}
\usage{
validate_plan(ap, df)
}
\arguments{
\item{ap}{Analysis plan data frame. Required columns: \code{variable},
\code{kobo_type}, \code{aggregation_method}, \code{disaggregation}. Optional column:
\code{repeat_for}.}

\item{df}{Data frame the plan will be run against.}
}
\value{
\code{TRUE} invisibly on success. Throws an error otherwise.
}
\description{
Run before any aggregation to surface common mistakes - missing columns,
typos in \code{kobo_type}, \code{aggregation_method} values that don't apply to the
given \code{kobo_type}, disaggregation columns that don't exist. Called
automatically by \code{\link[=analyze_survey]{analyze_survey()}}; exported so users can lint a plan
independently.
}
\details{
For \code{select_multiple} variables, the variable name need \strong{not} exist as a
string column in the data, so long as sibling binary columns are present
(see \code{\link[=detect_ms_options]{detect_ms_options()}}).
}
\examples{
df <- data.frame(gender = c("m","f"), age = c(30, 40))
ap <- data.frame(
  variable = c("gender", "age"),
  kobo_type = c("select_one", "integer"),
  aggregation_method = c(NA, "mean"),
  disaggregation = c("all", "all")
)
validate_plan(ap, df)

}
\seealso{
\code{\link[=analyze_survey]{analyze_survey()}}
}
