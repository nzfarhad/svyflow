% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci-options.R
\name{ci_opts}
\alias{ci_opts}
\title{Confidence-interval options for survey estimates}
\usage{
ci_opts(
  ci_level = 0.95,
  df = NULL,
  prop_method = NULL,
  interval_type = "mean",
  qrule = "math"
)
}
\arguments{
\item{ci_level}{Confidence level, a single number strictly between 0 and
\enumerate{
\item Default \code{0.95} (95\% CI). Sets the multiplier on the SE via the
\code{(1 + ci_level) / 2} quantile.
}}

\item{df}{Degrees of freedom for the interval. \code{NULL} (default) uses the
survey design's degrees of freedom (\code{survey::degf}), giving a
t-distribution interval. \code{Inf} uses the normal approximation
(multiplier \code{1.96} at 95\%). A finite number forces a specific t df.}

\item{prop_method}{Confidence-interval method for \strong{proportions}
(\code{select_one} / \code{select_multiple} only). \code{NULL} (default) keeps the
plain Wald interval on the 0/1 mean (\code{survey::svymean}), which can fall
outside \verb{[0, 1]} for rare outcomes. Set to one of \code{"logit"},
\code{"likelihood"}, \code{"asin"}, \code{"beta"}, \code{"mean"}, \code{"xlogit"} to use
\code{survey::svyciprop}-style bounded intervals (\code{"logit"} is the usual
recommendation for rare indicators). Ignored by numeric aggregators.}

\item{interval_type, qrule}{Passed to \code{\link[srvyr:survey_quantile]{srvyr::survey_quantile()}} for
\strong{quantile} statistics (\code{median}, quartiles, \code{\link[=summarize_quantile]{summarize_quantile()}}).
Defaults \code{"mean"} / \code{"math"} match srvyr's own defaults. Ignored by
non-quantile aggregators.}
}
\value{
A validated list of class \code{svyflow_ci_opts}.
}
\description{
Bundles the knobs that control how standard errors and confidence
intervals are produced by the underlying \pkg{srvyr} / \pkg{survey}
engine. Pass the result to \code{\link[=analyze_survey]{analyze_survey()}} or to any of the
\verb{summarize_*()} wrappers via their \code{ci} argument. The defaults reproduce
the package's historical behaviour exactly, so existing code is
unaffected.
}
\examples{
# Defaults (== current behaviour: 95\%, design-df t interval, Wald props)
ci_opts()

# Normal-approximation 90\% intervals
ci_opts(ci_level = 0.90, df = Inf)

# Logit proportions (better for rare outcomes near 0\% / 100\%)
ci_opts(prop_method = "logit")

}
\seealso{
\code{\link[=analyze_survey]{analyze_survey()}}, \code{\link[=summarize_select_one]{summarize_select_one()}}
}
