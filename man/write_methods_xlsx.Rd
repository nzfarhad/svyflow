% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-methods-xlsx.R
\name{write_methods_xlsx}
\alias{write_methods_xlsx}
\title{Export a standalone methods / summary workbook}
\usage{
write_methods_xlsx(
  file,
  design,
  ci = ci_opts(),
  result_format = "proportion",
  digits = 1,
  plan = NULL,
  results = NULL,
  cover_notes = NULL,
  theme = xlsx_theme()
)
}
\arguments{
\item{file}{Output path ending in \code{.xlsx}.}

\item{design}{The \link[srvyr:tbl_svy]{srvyr::tbl_svy} used for the analysis (the value
passed to \code{\link[=analyze_survey]{analyze_survey()}}).}

\item{ci}{A \code{\link[=ci_opts]{ci_opts()}} bundle (or a list of its arguments). Documents
the CI method used.}

\item{result_format, digits}{As passed to \code{\link[=analyze_survey]{analyze_survey()}}. Overridden
when \code{results} carries them as attributes.}

\item{plan}{Optional analysis-plan data frame. When supplied, the sheet
gains a "Plan" section with indicator counts by \code{kobo_type} and
\code{aggregation_method}.}

\item{results}{Optional \code{\link{svyflow_results}} tibble. When supplied with
a \code{DEFF} column, a "Design effect (DEFF)" section summarises the
distribution and lists the five highest DEFFs.}

\item{cover_notes}{Optional character vector of free-text lines —
project name, funder, contact, footnote disclaimers, etc. When
supplied, rendered as a "Project" section near the top of the
sheet. Each element becomes one line. Named entries render as
\code{"<name>: <value>"}; unnamed entries render verbatim.}

\item{theme}{An \code{\link[=xlsx_theme]{xlsx_theme()}} object controlling fonts, colours, and
section styling.}
}
\value{
The output \code{file} path, invisibly.
}
\description{
Writes a single-sheet \code{.xlsx} workbook documenting how an analysis was
produced: session info, the survey design specification, sample-size
counts, weights summary, CI options, result format, and (optionally) a
plan summary and a DEFF roll-up. Intended as a companion artefact to
\code{\link[=write_xlsx]{write_xlsx()}} so a recipient can interpret and reproduce the results
workbook.
}
\details{
The function is deliberately standalone — it does not touch
\code{\link[=analyze_survey]{analyze_survey()}} or \code{\link[=write_xlsx]{write_xlsx()}} and produces its own workbook.

If \code{results} is supplied, its stamped \code{result_format} / \code{digits}
attributes are read in preference to the explicit arguments, reducing
the risk that the methods sheet drifts away from what was actually
computed. If the result carries a \code{DEFF} column, a roll-up section is
added.
}
\examples{
\donttest{
df <- data.frame(
  gender = sample(c("m", "f"), 200, TRUE),
  weight = runif(200, 0.5, 2)
)
des <- make_design(df, weights = "weight")
ap  <- tibble::tribble(
  ~variable, ~kobo_type,   ~aggregation_method, ~disaggregation,
  "gender",  "select_one", NA,                  "all"
)
res <- analyze_survey(des, ap, deff = TRUE)
write_methods_xlsx(tempfile(fileext = ".xlsx"),
                   design = des, plan = ap, results = res)
}

}
\seealso{
\code{\link[=write_xlsx]{write_xlsx()}}, \code{\link[=analyze_survey]{analyze_survey()}}, \code{\link[=ci_opts]{ci_opts()}}
}
