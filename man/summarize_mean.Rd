% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{summarize_mean}
\alias{summarize_mean}
\title{Survey-design-aware mean of a numeric indicator}
\usage{
summarize_mean(
  design,
  variable,
  disaggregation = NULL,
  variable_label = NULL,
  disaggregation_label = NULL,
  digits = NULL,
  result_only = FALSE
)
}
\arguments{
\item{design}{A \link[srvyr:tbl_svy]{srvyr::tbl_svy} survey design.}

\item{variable}{Character. The numeric column to summarise.}

\item{disaggregation}{Character or \code{NULL}. A grouping column name;
\code{NULL} (or \code{"all"}) means no disaggregation.}

\item{variable_label}{Display label for \code{variable}. Becomes the value
of the \code{Indicator} column. Falls back to \code{variable} when \code{NULL}.}

\item{disaggregation_label}{Display label for \code{disaggregation}. Used as
the column header for the disaggregation column. Falls back to
\code{disaggregation} when \code{NULL}.}

\item{digits}{Non-negative numeric scalar, or \code{NULL} for no rounding.
Passed through to \code{\link[=analyze_survey]{analyze_survey()}}; numeric statistics are not
rescaled, so rounding only takes effect in the \code{percent_fmt} form
(not generally useful here). Default \code{NULL}.}

\item{result_only}{If \code{TRUE}, return only \code{Indicator}, the disaggregation
column (if any), and the \code{Mean} column. \code{SE} / \verb{CI_*} / \code{Count} are
dropped.}
}
\value{
A tibble of class \code{svyflow_summary} with columns
\code{Indicator}, optionally the disaggregation column, then \code{Mean},
\code{SE}, \code{CI_low}, \code{CI_high}, \code{Count} (unless \code{result_only = TRUE}).
}
\description{
Computes the (design-correct) mean of \code{variable}, optionally
disaggregated. Returns a publication-ready tibble with the mean in a
column literally named \code{Mean}, plus \code{SE}, \code{CI_low}, \code{CI_high}, \code{Count}.
Use \code{result_only = TRUE} to drop the auxiliary columns for compact
output.
}
\examples{
df  <- data.frame(
  hh_size = stats::rpois(200, 5) + 1,
  gender  = sample(c("m","f"), 200, TRUE),
  weight  = runif(200, 0.5, 2.0)
)
des <- make_design(df, weights = "weight")

summarize_mean(des, "hh_size", variable_label = "Household size")
summarize_mean(des, "hh_size",
               disaggregation = "gender",
               variable_label = "Household size",
               disaggregation_label = "Sex")

}
\seealso{
\code{\link[=summarize_sum]{summarize_sum()}}, \code{\link[=summarize_median]{summarize_median()}}, \code{\link[=summarize_quantile]{summarize_quantile()}}

Other summarize: 
\code{\link{summarize_max}()},
\code{\link{summarize_median}()},
\code{\link{summarize_min}()},
\code{\link{summarize_quantile}()},
\code{\link{summarize_select_multiple}()},
\code{\link{summarize_select_one}()},
\code{\link{summarize_sum}()}
}
\concept{summarize}
